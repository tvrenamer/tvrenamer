language: java

# The build needs a JDK matching the toolchain in build.gradle.
jdk: openjdk21

os: linux
dist: jammy

# Some of the provider tests query tvmaze.com, so the build needs network access.
script: ./gradlew build

cache:
  directories:
  - $HOME/.gradle/caches/
  - $HOME/.gradle/wrapper/
